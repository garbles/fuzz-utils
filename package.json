{
  "name": "fuzz-utils",
  "version": "0.0.3",
  "author": "Gabe Scholz <github@gabe.pizza>",
  "license": "MPL-2.0",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf build",
    "test": "jest",
    "prepublish": "npm run test && npm run clean && npm run build"
  },
  "devDependencies": {
    "@types/jest": "^24.0.20",
    "@types/node": "^12.11.7",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0",
    "typescript": "^3.6.4"
  }
}
